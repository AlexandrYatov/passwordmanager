import { SearchCardPipe } from './search-card.pipe';

describe('SearchCardPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCardPipe();
    expect(pipe).toBeTruthy();
  });
});
